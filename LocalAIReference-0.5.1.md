# Local AI macOS Reference 0.5.1

This hotfix prevents the chat from crashing after a PDF attachment is selected. Page and character counts are now converted to localized strings before they enter the attachment summary, and the localization API rejects non-string format arguments at compile time.

Document extraction, local tool permissions, model behavior, privacy boundaries, and the explicit-download policy are unchanged.
