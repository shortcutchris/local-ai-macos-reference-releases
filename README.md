# Local AI macOS Reference Releases

Signed and notarized application releases plus the stable Sparkle appcast for Local AI macOS Reference.

This repository intentionally contains no private source code, model files, prompts, transcripts, audio, signing keys, or credentials.

Installed applications read appcast.xml anonymously and accept an update only after Sparkle EdDSA verification.